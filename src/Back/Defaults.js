/**
 * Plugin constants (hardcoded configuration) for backend code.
 */
export default class Demo_Crudl_Back_Defaults {
    NAME = '@flancer64/tg-demo-crudl';

    constructor() {
        Object.freeze(this);
    }
}
